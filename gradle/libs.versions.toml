# =============================================================
# Версии зависимостей — единый источник правды.
# =============================================================

[versions]
# ---------- Build Tools ----------
agp = "9.1.1"

# ---------- AndroidX ----------
coreKtx = "1.19.0"
appcompat = "1.8.0"
material = "1.14.0"

# ---------- TWA ----------
# android-browser-helper — официальная библиотека Google для TWA.
# Версия 2.5.0 — стабильная.
androidbrowserhelper = "2.5.0"

# ---------- Тесты ----------
junit = "4.13.2"
junitVersion = "1.3.0"
espressoCore = "3.7.0"

[libraries]
# ---------- AndroidX ----------
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-appcompat = { group = "androidx.appcompat", name = "appcompat", version.ref = "appcompat" }
material = { group = "com.google.android.material", name = "material", version.ref = "material" }

# ---------- TWA ----------
androidbrowserhelper = { group = "com.google.androidbrowserhelper", name = "androidbrowserhelper", version.ref = "androidbrowserhelper" }

# ---------- Тесты ----------
junit = { group = "junit", name = "junit", version.ref = "junit" }
androidx-junit = { group = "androidx.test.ext", name = "junit", version.ref = "junitVersion" }
androidx-espresso-core = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espressoCore" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }